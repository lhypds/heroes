PORT=3181
PM2_NAME=heroes
# Public hostname(s) the page is served from. Comma-separate more than one.
HOST=
# For ./crawl.sh and the account check at /api. Leave empty if the GitHub CLI is
# signed in.
GITHUB_TOKEN=
